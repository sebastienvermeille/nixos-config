{ pkgs, ... }:

{
  home.stateVersion = "25.05";

  #programs.zsh.enable = true;
  programs.git.enable = true;
  programs.home-manager.enable = true;

  home.packages = with pkgs; [
    firefox
    brave
    #neovim
    #ripgrep
    #bat
  ];

  # Further user-specific configuration here
}

